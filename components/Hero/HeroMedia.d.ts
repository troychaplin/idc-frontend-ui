import { default as React } from '../../../node_modules/react';
export interface HeroMediaProps {
    children: React.ReactNode;
}
export declare const HeroMedia: {
    ({ children }: HeroMediaProps): import("react/jsx-runtime").JSX.Element;
    displayName: string;
};
